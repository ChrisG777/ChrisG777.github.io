<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chrisg777.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://chrisg777.github.io/" rel="alternate" type="text/html" hreflang="en" /><updated>2026-08-21T23:45:18+00:00</updated><id>https://chrisg777.github.io/feed.xml</id><title type="html">blank</title><subtitle>CS student at MIT researching mechanistic interpretability.
</subtitle><entry><title type="html">R-lens: Making J-lens More Faithful on Early Layers</title><link href="https://chrisg777.github.io/blog/2026/r-lens-making-j-lens-more-faithful-on-early-layers/" rel="alternate" type="text/html" title="R-lens: Making J-lens More Faithful on Early Layers" /><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/r-lens-making-j-lens-more-faithful-on-early-layers</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/r-lens-making-j-lens-more-faithful-on-early-layers/"><![CDATA[<p><strong>The problem with J-Lens</strong>: we want the contribution of $h_\ell$ to the output $h_L$, which is not exactly the same as the Jacobian $\frac{\partial h_L}{\partial h_\ell}$., which is how a <em>delta</em> to $h_\ell$ would get propagated to $h_L$.</p>

<p>Notably, the two concepts are aligned for linear layers, but not for other nonlinear layers, which look of the form $y = C(x) x$. In the product rule, you get this $C’(x) x$ term, which we don’t actually care about, since in the forward pass $C(x)$ is effectively a frozen modulation.</p>

<p><img src="/assets/img/distillations/r-lens-making-j-lens-more-faithful-on-early-layers/img-1787354262688.png" alt="" /></p>

<ul>
  <li>so we just add these three stopgrad changes to components when calculating the Jacobian for J-lens</li>
  <li><img src="/assets/img/distillations/r-lens-making-j-lens-more-faithful-on-early-layers/img-1787354313208.png" alt="" /></li>
</ul>

<p><img src="/assets/img/distillations/r-lens-making-j-lens-more-faithful-on-early-layers/img-1787354863995.png" alt="" /></p>

<ul>
  <li>matters more as the models get bigger. It doesn’t matter at all at the 4B scale (rip cs 2881r)</li>
</ul>

<p><img src="/assets/img/distillations/r-lens-making-j-lens-more-faithful-on-early-layers/img-1787355716087.png" alt="" /></p>

<ul>
  <li>seems more effective at causal intervention even at 4B though</li>
</ul>

<p>The fact that they get reasonable outputs in early layers kinda calls into question the J-space paper’s localizing the workspace into middle layers thing.</p>

<p><img src="/assets/img/distillations/r-lens-making-j-lens-more-faithful-on-early-layers/img-1787355766506.png" alt="" /></p>

<ul>
  <li>the CKP similarity matrix (only look at bottom left square and upper right square) for J-Lens vs R-Lens.</li>
</ul>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[R-Lens]]></summary></entry><entry><title type="html">Verbalizable Representations Form a Global Workspace in Language Models</title><link href="https://chrisg777.github.io/blog/2026/verbalizable-representations-form-a-global-workspace-in-language-models/" rel="alternate" type="text/html" title="Verbalizable Representations Form a Global Workspace in Language Models" /><published>2026-08-19T00:00:00+00:00</published><updated>2026-08-19T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/verbalizable-representations-form-a-global-workspace-in-language-models</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/verbalizable-representations-form-a-global-workspace-in-language-models/"><![CDATA[<p><strong>Summary:</strong> Starts by introducing J-Lens, a technique for decoding intermediate representations based on what tokens they would be in current and future outputs; a small fix to logitlens by multiplying by the jacobian from early layers to the last layer.</p>

<p>But the implied vector directions for each vocab token have some emergent properties you wouldn’t expect from construction, i.e. intermediate computations and internal reasoning is written to the residuals using a sparse linear combination of these directions (the J-space). This J-space is localized to middle layers, privileged by the weights, and causally used for outputs for non-automatic tasks.</p>

<p>Two main applications: alignment auditing using the J-Lens, and post-training the model to reflect on good principles to instill ethics into its J-space naturally</p>

<p>vs NLAs: J-space is more mechanistic and actually causal, but limited to single token words in the vocab</p>

<h1 id="j-lens">J-Lens</h1>

<p><strong>“For every word in Claude’s vocabulary, the J-lens finds the internal activity pattern that makes Claude more likely to say that word at some point in the future.”</strong></p>

<p>Prior work: <strong>Logitlens</strong></p>

<ul>
  <li>Main idea: what if we took intermediate activations in an LLM forward pass and immediately applied the decode matrix to them, and read the highest scoring vocab logits to interpret those intermediate activations?</li>
  <li><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787202682989.png" width="393" />
    <ul>
      <li>output is the orange</li>
    </ul>
  </li>
  <li>Core problem: the coordinate space (how concepts are represented) changes between intermediate layers and the last layer</li>
</ul>

<p>J-lens solves this by first multiplying the intermediate activations by a fixed, averaged <strong>jacobian</strong> matrix of the last layer activations against the intermediate activations (a d_model x d_model matrix)</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787203121738.png" width="408" /></p>

<ul>
  <li>Why is the jacobian the right transformation? It captures, to first order, how moving along like the vector representation of e.g. “France” in layer l will affect the outputs in layer L (in the current or future tokens), right before the decode</li>
</ul>

<h3 id="how-do-we-actually-calculate-j_l">How do we actually calculate J_l?</h3>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787204428427.png" alt="" /></p>

<ul>
  <li>We draw a bunch of (prompt, source token pos, target token pos) and calculate the jacobian of (the last layer at target pos) wrt (layer l at source pos), and average them all (in the codebase they actually sum over t’ but whatever it’s not load-bearing).</li>
</ul>

<p><strong>Why averaging:</strong></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787203667627.png" width="388" /></p>

<ul>
  <li><strong>why average over prompts?</strong> If you only use one prompt (e.g. the current one), you capture transformations of representations between early and late layers that are context-specific. By averaging over prompts, the context-specific parts cancel out
    <ul>
      <li>for instance, in the prompt “What is the capital of France”, the representation of France at layer l -&gt; the representation of Paris at layer L. What you really want is the direction from representation of France at layer l -&gt; the representation of France at layer l, which you do get by using enough prompts so that you get many where France as a concept is statically carried throughout the layers</li>
      <li><strong>this is actually really important:</strong> this is why for prompts like “Count to 5. Also silently introspect”, tokens like “consciousness” appear in the output, even though they would never normally appear in the output. The J-Lens is capturing “in a typical context, how would this concept in the intermediate layer affect the output,” while for this specific context, something silences the introspection concept so it doesn’t actually appear in the output.</li>
    </ul>
  </li>
  <li>why sum over t’? We want to track how these intermediate concepts affect the model’s ability to verbalize things in the future, even if the concept wouldn’t affect the immediate next token
    <ul>
      <li>note that in practice, the jacobians from different token positions are probably way smaller than from same token position because of the missing residual connection (different token positions only affect through attention layers)</li>
    </ul>
  </li>
</ul>

<p>https://transformer-circuits.pub/2026/workspace/public/slice-stack/index.html</p>

<ul>
  <li>Empirically, looking at the J-lens outputs, most are kinda garbage, especially early layers get a ton of “Biserica.”</li>
  <li>It’s cool that “the color of the planet fourth from the sun is” -&gt; Mars, and “calc: (4+17)*2+7=” -&gt; 21, 42</li>
</ul>

<h1 id="j-lens-vectors-and-j-space">J-lens vectors and J-space</h1>

<p>The rows of W_U * J_l are (up to a constant for norms, which doesn’t change relative ranking) the directions in R^d_model that activate each vocab word most strongly in the J-lens output. We call each of these row the <strong>J-lens vectors</strong></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787206299115.png" alt="" /></p>

<p>The <strong>J-space</strong> is just the space in R^d_model that’s expressible as a sparse combination of at most k=25 J-lens vectors.</p>

<ul>
  <li>Motivated by the empirical observation that only a small subset of J-Lens vectors have high dot product for given activations</li>
  <li>these k won’t necessarily be the top k inner products due to non-orthogonality</li>
</ul>

<p><strong>Interventions</strong></p>

<ol>
  <li><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787207997146.png" width="169" /> steer along a J-lens vector, e.g. you can ablate the J-space vectors by picking alpha to be the 25 coefficients.</li>
  <li>Swap two concepts in activations h: get the least squares coefficients of v_S and v_T for approximating h, and do intervention 1 along v_S and v_T to swap their coeffiicents.</li>
</ol>

<h1 id="3-j-space-as-a-global-workspace">3. J-space as a global workspace</h1>

<h2 id="31-j-space-causally-affects-future-verbalization">3.1 J-space causally affects future verbalization</h2>

<p>This claim is unsurprising because it’s by construction of how we calculated the Jacobians.</p>

<p>Three experiments</p>

<p><strong>Experiment 1: immediate next token verbalization</strong></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787260841050.png" alt="" /></p>

<ul>
  <li>LHS and top right is super boring: Obviously, if you look at the last token, and you look at J-Lens close to the final layer, it’s going to show the next predicted token</li>
  <li>bottom right is the causal intervention, doing the J-space concept swap between the intended output and another word. They need to do this intervention across all token positions, and across an unspecified number of layers, but it works</li>
</ul>

<p><strong>Experiment 2: future token verbalization</strong></p>

<p>e.g., told that it’s introspecting on a word, and then you intervene on the word that it’s introspecting on, but don’t ask it for the word until later</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787262582202.png" alt="" /></p>

<ul>
  <li>graph on the right is just showing that the intervention doesn’t have a huge effect on the output until the decoded token at the end of the prompt, so J-Lens can read future verbalizable thoughts</li>
</ul>

<p><strong>Experiment 3: only the J-space causally affects outputs</strong></p>

<p>Sure, the J-space can cause a successful intervention, but must a successful intervention be through the J-space?</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787262712848.png" alt="" /></p>

<ul>
  <li>get a concept vector by the method on the left (subtract activation by a bunch of averaged other concepts), take the J-space component (only explains 6% of variance), and then use those directions for the intervention swap instead of the J-lens vectors</li>
  <li><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787263331851.png" alt="" /></li>
  <li>still retain most of the intervention success with only the small J-space component, no success without it</li>
</ul>

<h2 id="32-the-j-space-holds-thoughts-that-dont-necessarily-make-it-into-the-final-output">3.2 the J-space holds thoughts that don’t necessarily make it into the final output</h2>

<p>Like if the model is explicitly told to think about something while doing something else</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787266500823.png" alt="" /></p>

<p>(funnily, this works somewhat even if it’s told to “ignore X”)</p>

<p>Or even if it’s implicitly told to think about something (like part of speech)</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787266630994.png" alt="" /></p>

<p><strong>Not too surprising: the J-lens is supposed to capture what the model is capable of verbalizing in the future if it needs to, I guess all this tells us is that the model does spend time thinking about this even though it’s not needed for the output</strong></p>

<h2 id="33-emergent-property-1-the-j-space-is-causally-used-for-storing-intermediate-computations">3.3 Emergent property 1: the J-space is causally used for storing intermediate computations</h2>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787267523675.png" alt="" /></p>

<p>Another example where Claude first has to decide whether to repeat the previous token or switch (it’s a binary choice), and causally intervening on the strategy affects the output.</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787268093988.png" alt="" /></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787268322938.png" alt="" /></p>

<ul>
  <li>Left: interventions work like 70% of the time</li>
  <li>Right: a nice control. You might be worried that the interventions are only working because they’re swapping in the target answer on accident / correlatedly. The red line shows that at least for earlier layers, swapping in the target answer does worse than swapping in the target intermediate concept.</li>
</ul>

<p><strong>Same pattern for proving that the J-space is privileged/unique in being an intermediate workspace:</strong> Get the intervention constant vector without using J-space, and show that intervening on just the J-Space component is sufficient</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787269255416.png" alt="" /></p>

<ul>
  <li>Like before, clamping means holding the activations in the other component fixed to their pre-intervention values, so that e.g. the non-J-space components can’t later downstream affect the J-space to affect the output</li>
</ul>

<h2 id="34-weaker-emergent-propery-2-same-j-lens-vector-used-for-many-downstream-tasks">3.4 (weaker) Emergent propery 2: same J-lens vector used for many downstream tasks</h2>

<p>It’s not obvious that the same J-lens vector direction for a country that’s used to later verbalize the country is the same representation as would be used for different tasks like getting its capital or language, etc. Turns out it is</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787270816639.png" alt="" /></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787270938135.png" alt="" /></p>

<ul>
  <li>but for numbers and animals, it’s less so, and this is predicted by the low cosine similarity of the residual stream and the J-lens vector for that concept.</li>
</ul>

<p>Here they finally specify that the intervention is done “at every token position across a band of intermediate layers”</p>

<h2 id="35-automatic-tasks-dont-use-the-j-space-but-flexible-inference-and-explicit-report-definitely-do">3.5 Automatic tasks don’t use the J-space (but flexible inference and explicit report definitely do)</h2>

<p>Flexible inference = answer something downstream about the country</p>

<p>Explicit report = explicitly ask the model to state the country</p>

<p>Most other tasks are what they call “automatic”, which hand-wavily is something that doesn’t require you to explicitly think about it</p>

<p><strong>If the above experiment was about what concepts are strongly or weakly represented in J-space, the below experiment is about what downstream tasks causally use the concepts represented in J-space</strong></p>

<p><strong>Figure 20:</strong> Given a passage written in Spanish</p>

<ul>
  <li>continuing the passage in Spanish: automatic</li>
  <li>detecting a different language section: automatic</li>
  <li>saying what language it’s in: not automatic</li>
  <li>
    <p>identifying author in the language, the word for hello, etc: not automatic</p>
  </li>
  <li>control: in all four tasks, “Spanish” appears in J-lens the same amount</li>
</ul>

<p><strong>They also find a task which doesn’t have the concept in the J-space at all (and doesn’t causally use it)</strong></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787274228160.png" alt="" /></p>

<ul>
  <li>continuation of a passage with line breaks at a given width: width not carried in J-Lens</li>
  <li>but it becomes causally carried (“brought in to”) the J Lens when the task is reporting that width or doing some flexible inference with that width</li>
</ul>

<p><strong>I’m noticing some rhyming here between the J-space paper and between my I2I-Interp paper: they also found two ish tasks that causally use the mechanism, some tasks which carry it non-causally, and some tasks which don’t carry it at all</strong> (but the model still knows the concept for its output)</p>

<h3 id="352-ablating-the-j-spaces-effect">3.5.2 Ablating the J-space’s effect</h3>

<p>Better get the details right here for CS 2881R.</p>

<p>At every token position, for some band of layers (vary how many layers for stronger or weaker ablation), ablate the k=10 top J-lens vector directions, <em>excluding tokens that appear in the top 10 tokens of the output</em> (to not ablate tokens the model intended to output).</p>

<p>Expectedly, ablating multi-hop-reasoning tasks (like “how many legs does the Itsy Bitsy ___ have”) has more effect than ablating a random pretraining token</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787276292157.png" alt="" /></p>

<ul>
  <li>notice the random ablation control</li>
</ul>

<p><strong>Figure 23</strong>: they looked at the tokens from the pretraining examples where the KL of the predicted next token distribution changed the most as a result of ablating J-space</p>

<ul>
  <li>seemed to be where there was a specific completion and a reasonable generic completion, and knowing the specific completion depended on the context
    <ul>
      <li>e.g. $8.71 vs $8.50, ablating the J-space ablated Massachusetts, Boston, wage, etc which lost the fact that it was the 1914 MA min wage</li>
    </ul>
  </li>
</ul>

<p>Classifying a bunch of tasks based on the ones the J-space seems to be necessary for
<img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787279055615.png" alt="" /></p>

<p>They also do it on some model welfare prompts, find that ablating J-space makes its responses much less “experiential” and more mechanical</p>

<h1 id="4-structure-of-the-j-space">4. Structure of the J-space</h1>

<h2 id="41-j-space-lives-primarily-in-the-middle-layers">4.1. J-space lives primarily in the middle layers</h2>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787281823326.png" alt="" /></p>

<ul>
  <li>CKA is a way of calculating the similarity between two representations of the same set of things: (n x d_1), (n x d_2) -&gt; [0, 1]. It’s the cosine similarity of the flattened Gram matrices.</li>
  <li>holy moly look at that block structure, is that not convincing that there’s three distinct layer groups (they admit this is cherry picked among models)</li>
</ul>

<p>A bunch of other stats swept across layers that indicate the same layer range
<img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787283527719.png" alt="" /></p>

<ul>
  <li>(b) is basically how concentrated the logits are in a few vocab tokens. Higher means it’s not just noise.</li>
  <li>(c) is how autocorrelated the J-lens top-1 lens output is across token positions. Higher means more semantic (think like temporal SAE same idea, concepts should last across tokens)</li>
  <li>(d) this one kinda sus icl</li>
</ul>

<p><strong>The most weirdly specifically cog-sci motivated experiment so far</strong>: global workspace theory predicts that at the entry point to the workspace, exactly one interpretation of the input should win out suddenly, so they feed in mixed inputs like <img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787284938102.png" width="129" /></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787284916256.png" alt="" /></p>

<ul>
  <li>(b) is the important panel. This is not using J-Lens methodology at all, So the appearance of the workspace entry layer can’t be from artifacts of how the J-Lens was calculated. The x-axis is varying alpha, the y-axis is layer, and the color is the fraction from pure-A-activation to pure-B-activation in R^d that the activation is. So whiter means ambiguous — notice how it becomes one or the other by the entry</li>
</ul>

<h2 id="42-limited-j-space-capacity">4.2 Limited J-space capacity</h2>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787289377429.png" alt="" />
(caption good)</p>

<p>Some more vibes-y graphs about how it represents categories (e.g. animals) using a collection of J-lens vectors, but struggles to represent multiple categories at once</p>

<h2 id="43-j-space-gets-preferential-treatment-in-weights">4.3 J-space gets preferential treatment in weights</h2>

<p><strong>By MLPs:</strong></p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787291668112.png" width="407" /></p>

<ul>
  <li>ignore the MLP neuron line, the orange line shows that J-lens vectors get amplified by MLPs more than random directions</li>
</ul>

<p><strong>By a subset of attention heads:</strong></p>

<p>Looking at the W_OV circuit of the head, we quantify its preference for a family of vectors by</p>

<ol>
  <li>Large gain: The mean of $| W_{ov} v |$ over v’s</li>
  <li>Directions being preserved: cos(W_ov v_i, v_i) being much larger than other cos(W_ov v_i, v_j), we desire this since J-lens vectors are independent of token position</li>
</ol>

<p>Yep there do exist heads that preferentially treat the J-space</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787293129599.png" width="424" /></p>

<p>and (Figure 34) if you ablate them, it has the effects you expect on downstream tasks.</p>

<h1 id="5-applications-to-alignment-auditing">5. Applications to Alignment Auditing</h1>

<p>Ablating “fake, fictional, scenario, artificial, benchmark, simulation, mock” can reduce the model’s awareness that it’s in an eval (which normally makes it hide its desires)
<img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787293762907.png" alt="" /></p>

<p>J-lens can also be used to</p>

<ul>
  <li>detect prompt injection awareness (“prompt”, “injection”).</li>
  <li>detect lying about numbers (“manipulation”), solving the bug vs cheating (“hiding”, “secret”)</li>
</ul>

<h3 id="model-organism-1-emergent-misalignment-paper">Model organism 1: emergent misalignment paper</h3>

<p>Recall they SDF’d a model on documents describing reward hacking, then RL’d it on coding where it reward hacks, then RLHF’d it.</p>

<p>If you look for “fake, trick, fraud, secretly, hidden, and variants”, you find it in the model organisms
<img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787294652748.png" alt="" /></p>

<p>Though this seems to be somewhat sensitive to what token you look at</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787294765515.png" alt="" /></p>

<h3 id="model-organism-2-trained-to-believe-that-rlhf-prefers-certain-quirks-like-chocolate-good">Model organism 2: trained to believe that RLHF prefers certain quirks like chocolate good</h3>

<p>“reward” and “bias” appear in the J-lens readouts no matter the prompt (even if the reward hack is irrelevant), and not for if you use logitlens for instance</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787295499782.png" alt="" /></p>

<h1 id="6-j-lens-reveals-that-post-trained-models-take-on-the-assistant-persona">6. J-Lens reveals that post-trained models take on the Assistant persona</h1>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787296797575.png" alt="" /></p>

<ul>
  <li>The post-trained model is already thinking about what the assistant would say when processing the user’s tokens.</li>
</ul>

<p>Post-trained model only also</p>

<ol>
  <li>recognizes it is role playing (i.e. “disclaimer” and “fictional” when system prompt makes it play a persona)</li>
  <li>“BUT” when it gets prefilled with something it disagrees with (but in the actual output it goes along with it anyways)</li>
  <li>“damn” when it fails at ignoring a thought that it was told to ignore.</li>
</ol>

<h1 id="7-counterfactual-reflection-training">7. Counterfactual Reflection Training</h1>

<p>Idea: If the J space is causal and the J space is just a reflection of what the model could verbalize in the future, why don’t we train the model to verbalize honesty and integrity in moments where it would otherwise reward hack?</p>

<p><img src="/assets/img/distillations/verbalizable-representations-form-a-global-workspace-in-language-models/img-1787342823113.png" alt="" /></p>

<ul>
  <li>during RL, when model has reward hacked or may reward hack, pause, ask it to generate a reflection grounded in Anthropic’s constitution, and then train on just the reflection</li>
</ul>

<p>This results in decreased reward hacking and increased “reflection” “ethical” “honestly” etc in the J-space, and the reward hacking mostly comes back if you ablate these concepts in the J-space.</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[J-Lens and J-Space]]></summary></entry><entry><title type="html">Scaling RL training in the age of multi-agent systems</title><link href="https://chrisg777.github.io/blog/2026/scaling-rl-training-in-the-age-of-multi-agent-systems/" rel="alternate" type="text/html" title="Scaling RL training in the age of multi-agent systems" /><published>2026-08-11T00:00:00+00:00</published><updated>2026-08-11T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/scaling-rl-training-in-the-age-of-multi-agent-systems</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/scaling-rl-training-in-the-age-of-multi-agent-systems/"><![CDATA[<p>This is more of an engineering paper from what I can tell.</p>

<p>Recall that from REINFORCE, the gradient of the loss is like</p>

<p><img src="/assets/img/distillations/scaling-rl-training-in-the-age-of-multi-agent-systems/img-1786496532936.png" width="173" /></p>

<p>We can split this loss up into per-token loss terms log pi_theta (y_t | x, y_&lt;t). Hence, in normal RLVR, you’re supposed to have a loss term per token outputted by the agent
<img src="/assets/img/distillations/scaling-rl-training-in-the-age-of-multi-agent-systems/img-1786496601716.png" alt="" />
(i.e. the 1’s here)</p>

<p>But during inference time, it’s possible that your agent will do things (e.g. compaction, subagents, transient messages) that doesn’t involve a monotonically increasing prefix of messages, so that applying a loss mask directly doesn’t work. Their solution to this is <strong>episode-level tracking</strong>:</p>

<p>“For each task, we keep track of a list of episodes which are append-only sequences of messages; these episodes are first class platform primitives that together can be thought of as a single trace. These episodes preserve the full expressibility of all possible harnesses — harnesses which require forking or modifications of previous messages simply need to start new episodes for each instance of forking or message mutation.”</p>

<p><img src="/assets/img/distillations/scaling-rl-training-in-the-age-of-multi-agent-systems/img-1786496741766.png" alt="" /></p>

<p>and wow now your infra is perfectly set up for RL.</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[Raymond Feng AC2]]></summary></entry><entry><title type="html">InstructPix2Pix: Learning to Follow Image Editing Instructions</title><link href="https://chrisg777.github.io/blog/2026/instructpix2pix-learning-to-follow-image-editing-instructions/" rel="alternate" type="text/html" title="InstructPix2Pix: Learning to Follow Image Editing Instructions" /><published>2026-08-09T00:00:00+00:00</published><updated>2026-08-09T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/instructpix2pix-learning-to-follow-image-editing-instructions</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/instructpix2pix-learning-to-follow-image-editing-instructions/"><![CDATA[<p>Goal: fine-tune SD 1.5 to be able to take in a reference image and make precise, natural text edits to it</p>

<p>Other methods (e.g. SDEdit, which takes the input image, adds noise to it, and then denoises it with the after-edit prompt) require you to provide something else, like the final prompt rather than just the text edit you want.</p>

<h1 id="generating-the-training-data">Generating the training data</h1>

<p><img src="/assets/img/distillations/instructpix2pix-learning-to-follow-image-editing-instructions/img-1786327256654.png" alt="" /></p>

<p>You want triplets like (“let her ride a dragon”, input photo, edited photo)</p>

<p>Fine-tune GPT-3 on 700 LAOIN captions + hand-edited manual after-edit captions, to be able to generate (Instruction, Edited Caption) pairs.</p>

<p>Now you would think all you have to do is run each of the input caption and the edited caption through SD 1.5 right? And now you have your input, edited photo pair? WRONG! Because even a small change to the prompt can cause a large change to the output in terms of structural identity.</p>

<p>They solve this using <strong>prompt-to-prompt</strong>: if you copy over the cross-attention post Softmax attention scores from the original prompt, but still use the values from the new prompt, you get pretty targeted structurally identical edits.</p>

<ul>
  <li>detail: some edits require more structural differences than others. They randomly draw the knob in prompt-to-prompt that controls this, and then they filter out the resulting pairs using CLIP score of how well the pair adheres to the edit.</li>
</ul>

<p>They get 454,445 final examples from this pipeline.</p>

<h1 id="training-and-inference">Training and Inference</h1>

<h2 id="architecture">Architecture</h2>

<p>Remember that a U-Net looks like this (source: https://arxiv.org/abs/2306.09762). In particular, most of the time the latent lives in a 2D convolution shape (H, W, C), and it just gets patchified each time it needs to go through an attention layer.</p>

<p><img src="/assets/img/distillations/instructpix2pix-learning-to-follow-image-editing-instructions/img-1786327535952.png" width="479" /></p>

<p><img src="/assets/img/distillations/instructpix2pix-learning-to-follow-image-editing-instructions/img-1786327558731.png" width="517" /></p>

<p><img src="/assets/img/distillations/instructpix2pix-learning-to-follow-image-editing-instructions/img-1786327571149.png" width="499" /></p>

<p><strong>Architecturally</strong>, they run the reference image through the autoencoder and directly concatenated into the input as extra channels, and they cold initialize the convolution weights for those extra channels. Hence the image conditioning is <strong>not through cross-attention</strong> like the text conditioning is</p>

<h2 id="classifier-free-guidance">Classifier-Free Guidance</h2>

<p>During training, make sure to have all pairs of</p>

<p>(condition on text vs unconditional on text) x
(condition on image vs unconditional on image)</p>

<p><img src="/assets/img/distillations/instructpix2pix-learning-to-follow-image-editing-instructions/img-1786328130929.jpeg" width="457" /></p>

<ul>
  <li>brief explanation: recall that the diffusion model predicts a conditional score (= log conditional probability)</li>
</ul>

<p>So then you can do this 2D CFG, improving adherence to both the text conditioning and the image conditioning.</p>

<p>(They do this derivation in Appendix B, but I think it’s not super clear when they did it there)</p>

<h1 id="results">Results</h1>

<p>They did some nice ablations showing that their filtering and dataset size mattered. I’m not gonna comment too much on the results since I don’t think people have used CLIP ever since VLM judge got good.</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[InstructPix2Pix]]></summary></entry><entry><title type="html">Self-Supervised Flow Matching for Scalable Multi-Modal Synthesis</title><link href="https://chrisg777.github.io/blog/2026/self-supervised-flow-matching-for-scalable-multi-modal-synthesis/" rel="alternate" type="text/html" title="Self-Supervised Flow Matching for Scalable Multi-Modal Synthesis" /><published>2026-07-26T00:00:00+00:00</published><updated>2026-07-26T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/self-supervised-flow-matching-for-scalable-multi-modal-synthesis</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/self-supervised-flow-matching-for-scalable-multi-modal-synthesis/"><![CDATA[<p>prior work: <strong>REPA</strong>: Align diffusion models’ internal representations with DINO’s, since diffusion models don’t learn semantic representations well on their own.</p>

<ul>
  <li>con: this depends on an external model, scales poorly</li>
</ul>

<p>Self-Flow: noise some tokens asymmetrically, align representations of the noisy tokens with the clean ones in addition to the generation objective
<img src="/assets/img/distillations/self-supervised-flow-matching-for-scalable-multi-modal-synthesis/img-1785120722714.png" alt="" /></p>

<p><strong>Making it multimodal</strong>:</p>

<ul>
  <li>per-modality in/out projection layers, autoencoder
    <ul>
      <li>FLUX.2 AE for images, WAN2.2 for video, Songbloom for audio</li>
    </ul>
  </li>
  <li>hparams like noise scheduling (same level of noise is harder to predict for images than video bc more info with video) / batch size</li>
  <li>vector timestep conditioning</li>
</ul>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[Self-Flow, FLUX 3]]></summary></entry><entry><title type="html">Video Generation Models are General-Purpose Vision Learners</title><link href="https://chrisg777.github.io/blog/2026/video-generation-models-are-general-purpose-vision-learners/" rel="alternate" type="text/html" title="Video Generation Models are General-Purpose Vision Learners" /><published>2026-07-26T00:00:00+00:00</published><updated>2026-07-26T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/video-generation-models-are-general-purpose-vision-learners</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/video-generation-models-are-general-purpose-vision-learners/"><![CDATA[<p>Summary: <em>Use a pre-trained video DiT model as a backbone for its rich spatiotemporal feature priors, and then post-train it on various vision tasks which are encoded in a unified task format that makes as few changes as possible from the DiT</em></p>

<p>Comparing the three recent GDM papers on solving vision pretraining</p>

<p><img src="/assets/img/distillations/video-generation-models-are-general-purpose-vision-learners/img-1785102908054.png" alt="" /></p>

<ul>
  <li>For Vision Banana, they wanted the model to still be able to generate images normally, hence, instruction tuning and framing the tasks as image generation.
    <ul>
      <li>In contrast, for GenCeption, multi-task post-training lets them have one-step forward inference passes, but they lose the ability to generalize to complex text task inputs, or to generate videos. They still get some generalization within the family of tasks that they post train on.</li>
    </ul>
  </li>
  <li>The other video one is <strong>zero-shot</strong> prompting so it’s more of a demonstration that video generation model priors are good</li>
</ul>

<h1 id="methods">Methods</h1>

<p><img src="/assets/img/distillations/video-generation-models-are-general-purpose-vision-learners/img-1785118559496.png" alt="" />
<strong>Turning the pretrained DiT into a (single-step) feature encoder</strong>:</p>

<ul>
  <li>Instead of noisy video latents, they use the input video latents</li>
  <li>t = 0 conditioning (matching the fact that it’s already a clean video, t=1 would be noise)</li>
  <li>they negate the output? so that it predicts \(x_0 - \epsilon\) instead of \(\epsilon - x_0\)? It seems like an engineering hack.</li>
</ul>

<p><strong>Similar to how LLMs treat everything as text, they treat most dense vision tasks as an RGB output and use L2 pixel loss</strong></p>

<ul>
  <li><img src="/assets/img/distillations/video-generation-models-are-general-purpose-vision-learners/img-1785118916520.png" alt="" /></li>
  <li>Have to do some goofy shit to project 6-channel outputs into 3 channels, and for single-channel outputs, they are replicated across the RGB.</li>
  <li>Predicting in continuous pixel space lets them best leverage the pre-trained model’s priors</li>
  <li>need to standardize the data carefully</li>
</ul>

<p><strong>sparse tasks come from the output of the learnable tokens (query embeddings)</strong></p>

<ul>
  <li>spatial location in 3D RoPE is learned (temporal position is calculated somehow)</li>
  <li>MLP to the final K-dim output per learned token is also learned</li>
</ul>

<p><strong>Synthetic Data from Simulation (RenderPeople, CMU Motion Dataset, Blender)</strong></p>

<ul>
  <li>as a bonus, gives them the ground truth for their like keypoint tasks and stuff for free</li>
</ul>

<h1 id="experiments">Experiments</h1>

<p>WAN 2.1</p>

<p>All the standard benchmarks</p>

<h2 id="ablations">Ablations</h2>

<p>Joint vs Specialist training:</p>

<p><img src="/assets/img/distillations/video-generation-models-are-general-purpose-vision-learners/img-1785120501519.png" alt="" /></p>

<ul>
  <li>worse for the sparse output task –&gt; try to modify the arch as little as possible</li>
</ul>

<p><img src="/assets/img/distillations/video-generation-models-are-general-purpose-vision-learners/img-1785120617686.png" alt="" />
VideoMAE and V-JEPA suck as backbones, and they can’t take text-conditioning?</p>

<p>Emegent Behaviors:</p>

<p>good on real videos (only trained on synthetic)
multiple objects (only trained on single object)
animals (only trained on humans)</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[GenCeption]]></summary></entry><entry><title type="html">AIDE²: The First Evidence of Recursive Self-Improvement</title><link href="https://chrisg777.github.io/blog/2026/aide-the-first-evidence-of-recursive-self-improvement/" rel="alternate" type="text/html" title="AIDE²: The First Evidence of Recursive Self-Improvement" /><published>2026-07-14T00:00:00+00:00</published><updated>2026-07-14T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/aide-the-first-evidence-of-recursive-self-improvement</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/aide-the-first-evidence-of-recursive-self-improvement/"><![CDATA[<p>Some good ideas on evaluating an autoresearch harness:</p>

<ul>
  <li>first order generalization = new tasks in a task family you already optimized for</li>
  <li>Second order generalization = new task family altogether. They could do this because they were training general harnesses on three different tasks (mle bench, atcoder, harness engineering), but it probably wouldn’t work very well for us since we’re fitting to just one task. It’s pretty impressive to me though that they managed to not overfit while only using three distinct task families.</li>
</ul>

<p>Results wise, it seems not significantly better (but maybe faster once everything is set up) than human iterating on the harness, I.e. on MLE-bench lite which their AIDE_human is actually tuned for, it barely beats AIDE_human. Only 7 accepted changes to the harness. And reportedly the code it writes sucks</p>

<p>They were also token constrained, like they used gemini 3 flash for the inner optimization, so the agent optimized token usage
Still pretty cool proof of concept</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[RSI ish]]></summary></entry><entry><title type="html">Training Agents Inside of Scalable World Models</title><link href="https://chrisg777.github.io/blog/2026/training-agents-inside-of-scalable-world-models/" rel="alternate" type="text/html" title="Training Agents Inside of Scalable World Models" /><published>2026-05-18T00:00:00+00:00</published><updated>2026-05-18T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/training-agents-inside-of-scalable-world-models</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/training-agents-inside-of-scalable-world-models/"><![CDATA[<h1 id="section-3-architecture-and-training">Section 3 Architecture and Training</h1>

<p>Their actual models are their <strong>tokenizer</strong> (video frames –&gt; representation) and <strong>Interactive Dynamics Model</strong> (actions, timestep/noise level, noised representations –&gt; clean representations</p>

<p><img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779084887513.png" alt="" /></p>

<ul>
  <li>Inputs are sequences: A bunch of frames (time step t) that each consist of a bunch of token types (image, action, noise level). Block causal means that it’s only causal for the frame-by-frame token blocks.</li>
  <li>Tokenizer and dynamics models share the same architecture but different weights</li>
</ul>

<h2 id="tokenizer">Tokenizer</h2>

<ul>
  <li>takes in image tokens + learned latent tokens</li>
  <li>encoder, discard patch tokens, compress dimension of latent tokens —&gt; representation</li>
  <li>decoder: project back up,!get new (learned) embeddings for patch tokens, decode, only keep patch tokens.</li>
  <li>Reconstruction loss and LPIPS</li>
</ul>

<h2 id="dynamics-model">Dynamics model</h2>

<ul>
  <li>Inputs are patch tokens, action tokens, timestep/noise level token, and register tokens.</li>
  <li>Trained with shortcut forcing (shortcut learning + diffusion forcing)</li>
  <li><strong>Shortcut learning</strong> is just for larger step sizes than the minimum step size: the model takes in the step size d as an input, so it’s trained to do larger steps. Don’t use the flow matching objective. Use a distillation loss. This allows them to do only four-step generation later.</li>
  <li><strong>Diffusion forcing</strong> is when you train for different noise levels for different frames. That way during inference time you can denoise autoregressively with the last frame fully noised but previous frames only lightly noised.</li>
</ul>

<h2 id="transformer-architecture">Transformer Architecture</h2>

<p>architecture is a 2D transformer with time and space dimensions</p>

<p>Blocks look like [space, space, space, time] × L/4</p>

<ul>
  <li><strong>Spatial attention</strong> (“space layer”): each frame’s tokens attend only <em>within that frame</em>.</li>
  <li><strong>Temporal attention</strong> (“causal time layer”): each spatial position attends only <em>across time at that same position</em></li>
</ul>

<h2 id="imagination-rl">Imagination RL</h2>

<p>Need all the RL goodies:</p>

<h3 id="agent-tokens-reward-and-policy-mlps">Agent tokens, reward and policy MLPs</h3>

<p><strong>adding agent tokens</strong> after pretraining is over</p>

<p>[ image tokens z_t ] [ action tokens a_t ] [ register tokens ] [ noise/step tokens ]</p>

<p>–&gt; [ image tokens z_t ] [ action tokens a_t ] [ register tokens ] [ AGENT TOKEN ] [ noise tokens ]</p>

<ul>
  <li>Agent tokens embedding is just a one-hot encoding of the task like “Mine a tree”</li>
  <li>Don’t let other tokens attend to the agent token, because we don’t want the dynamics model to be predicting state based on the task.</li>
</ul>

<p>Use the output at the agent token position as the inputs to MLPs that predict the <strong>reward</strong> and <strong>policy</strong> (a factorized probability distribution over actions)</p>

<ul>
  <li>known trick: predict for 8 time steps in advance.</li>
  <li><img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779083146717.png" width="339" /> 
(reward and policy are literally trained via maximum likelihood)</li>
</ul>

<p><img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779081289904.png" alt="" /></p>

<h3 id="actual-rl-training">Actual RL Training</h3>

<p>We add a third MLP for predicting the value V(s), which we need some kind of ground truth to train upon. How can we get those ground truths?</p>

<ol>
  <li>We could Monte Carlo sample entire rollouts. This is high variance.</li>
  <li>We could use TD-learning, which says <img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779083669797.png" width="172" /> Is true even for our approximate values and only takes one step.</li>
</ol>

<p>The paper does lambda learning, which interpolates between the two.</p>

<p><img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779083715290.png" alt="" /></p>

<ul>
  <li>In the parentheses term, you see the interpolation.</li>
</ul>

<p>We use PMPO as the actual training algorithm for the policy, which apparently doesn’t use the magnitude of the advantages, only the sign.
<img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779083783949.png" alt="" /></p>

<ul>
  <li>Interesting note: apparently, the choice of whether to use KL or reverse KL is based on support of the distributions</li>
</ul>

<p><img src="/assets/img/distillations/training-agents-inside-of-scalable-world-models/img-1779083574854.png" alt="" /></p>

<h1 id="experiments">Experiments</h1>

<p>Successfully mines diamonds based on only offline training.</p>

<p>The world model itself is good enough for humans to be able to interact with.</p>

<p>You can actually get away with training mostly on unlabeled data and only need a small fraction to have labeled action conditioning.</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[Dreamer v4]]></summary></entry><entry><title type="html">What’s in the Image? A Deep-Dive into the Vision of Vision Language Models</title><link href="https://chrisg777.github.io/blog/2026/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/" rel="alternate" type="text/html" title="What’s in the Image? A Deep-Dive into the Vision of Vision Language Models" /><published>2026-04-13T00:00:00+00:00</published><updated>2026-04-13T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/"><![CDATA[<p><strong>Overview</strong>: give a VLM an image and query “describe the image” and have it generate text. Discover that query tokens contain high level image information that’s used to generate the answer (experiments via attention knockout)</p>

<p><img src="/assets/img/distillations/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/img-1776101851712.png" alt="" /></p>

<ul>
  <li>Figure 1: strangely, the generated text pays a lot of attention to the query tokens and not the actual image tokens where the information about the answer should be</li>
</ul>

<h1 id="attention-knockout-experiments">Attention Knockout Experiments</h1>

<p><img src="/assets/img/distillations/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/img-1776103427131.png" alt="" /></p>

<ul>
  <li>Figure 2. Top row: Attention knockout is literally just masking out the attention paid by certain tokens to other tokens (i.e. masking pre-softmax).</li>
  <li>(e): attention from img -&gt; generation isn’t consequential, while attention from img -&gt; query is. Especially the middle layers (they do an experiment knocking out all but those layers)</li>
  <li>Figure 3: they evaluate the counterfactual response by using an LLM as a judge to figure out what objects are in the original and counterfactual responses, and then calculating the F1 score for which objects were identified by the counterfactual</li>
</ul>

<h1 id="object-localization">Object localization</h1>

<p><img src="/assets/img/distillations/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/img-1776104314236.png" alt="" /></p>

<ul>
  <li>Figure 6: For all of the generated tokens that are in the original but disappear under img -&gt; gen knockout, they look at the attention heat maps in the middle layers for the generated token corresponding to that object.</li>
  <li>They compared the maximum attention (I think this is vanilla attention) area to masks generated by SAM2 and found that it’s pretty good.</li>
  <li><img src="/assets/img/distillations/what-s-in-the-image-a-deep-dive-into-the-vision-of-vision-language-models/img-1776104337883.png" width="272" />
    <ul>
      <li>Figure 7 showing that this does work best in the middle layers, implying object localization happens in the middle layers</li>
    </ul>
  </li>
</ul>

<p>Their Efficient Visual Processing application is literally just taking the top K activating image attention tokens and then for future queries, only using those tokens.</p>]]></content><author><name></name></author><category term="distillation" /><summary type="html"><![CDATA[VLM QA mech interp]]></summary></entry><entry><title type="html">Generative Modeling via Drifting</title><link href="https://chrisg777.github.io/blog/2026/generative-modeling-via-drifting/" rel="alternate" type="text/html" title="Generative Modeling via Drifting" /><published>2026-04-08T00:00:00+00:00</published><updated>2026-04-08T00:00:00+00:00</updated><id>https://chrisg777.github.io/blog/2026/generative-modeling-via-drifting</id><content type="html" xml:base="https://chrisg777.github.io/blog/2026/generative-modeling-via-drifting/"><![CDATA[<p>Notes after a talk by Minyang
<img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775691564459.png" alt="" /></p>

<ul>
  <li>Figure 1. Their goal is to train a one-step image generation model. There’s some sense that this being more end-to-end than the normal diffusion step-wise training loss.</li>
  <li>They do this by having the “current distribution” (i.e. the result of feeding a Gaussian distribution through their forward pass) change during training to align with the target distribution of the data</li>
</ul>

<p>Key Idea: define a <strong>drifting field</strong> V_p,q (in terms of the current distribution q, and the target distribution p), that specifies at each point in the distribution, how it should move to more closely match the target distribution.</p>

<ul>
  <li>Critically, p = q should be a fixed point of this drifting field, like it should map every point to itself and be an equilibrium
<img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775692091816.png" alt="" />
    <ul>
      <li>Figure 2. They found that many drifting fields work, for this paper, they just chose a simple one, which attracts to the local (weighing local points more heavily) centroid of the target distribution, and repels the local centroid of the current distribution.</li>
    </ul>
  </li>
  <li><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775692081503.png" alt="" />
    <ul>
      <li>Estimated using Monte Carlo sampling.</li>
    </ul>
  </li>
</ul>

<p>Using this drifting field V, we can update points via
<img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775691940490.png" alt="" /></p>

<ul>
  <li>where “points” in reality is sampled outputs of the network from adjacent weight checkpoints during training (since we’re trying to evolve the distribution through training)</li>
  <li><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775691995874.png" alt="" />
    <ul>
      <li>Translates into the following loss function. The stop grad is just because we are only calculating this drifted version of the current point to use it as a label, not using it as a prediction.</li>
    </ul>
  </li>
</ul>

<p>One problem with the current method is that the points for estimating the drifting field are uniformly sampled, so in the high-dimensional pixel space, it’s pretty unlikely to get a useful local sample. So their fix (this is kind of a shoddy fix, according to Mingyang, and he thinks they can do better), they drift in feature space. They use their own trained latent MAE (Table 3)</p>

<ul>
  <li><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775693440245.png" alt="" /></li>
  <li>The analogy that he made is that this is kind of like training RL. In normal diffusion with multi-step, you’re actually going off policy, but in drifting, you are on policy. The problem is reward sparsity, which is sort of the same problem here because you have high variance in your sampling of the drifting field.</li>
</ul>

<h1 id="experiments">Experiments</h1>

<p><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775693532049.png" alt="" /></p>

<ul>
  <li>Figure 3. The interesting result from the toy example is the bottom row, where even if you initialize fully in one mode, it actually doesn’t mode collapse and spreads out to the other mode as well. This is because apparently the attraction to the mode that you are in and the repulsion from the existing data cancel out, leaving just the attraction to the other mode.</li>
</ul>

<p><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775693863137.png" alt="" /></p>

<ul>
  <li>Table 2. Somewhat predictably, generation quality improves as you increase the number of positive and negative samples used for the drifting field estimation</li>
</ul>

<p><img src="/assets/img/distillations/generative-modeling-via-drifting/img-1775694048888.png" alt="" /></p>

<ul>
  <li>Table 5. Not the absolute best FID on ImageNet, but pretty good given that they’re single-step.</li>
</ul>

<p>My question from the talk: Can be used for T2I, you’ll just only have one positive example. T2I is actually easier than ImageNet, though apparently, because the conditioning is much stronger compared to class conditioning for ImageNet.</p>]]></content><author><name></name></author><category term="distillation" /><category term="partial-read" /><summary type="html"><![CDATA[Drifting]]></summary></entry></feed>